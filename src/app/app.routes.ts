import { Routes } from '@angular/router';
import { HeroisTableComponent } from './components/herois-table/herois-table.component';

export const routes: Routes = [
     { path: 'home', component: HeroisTableComponent }
];
